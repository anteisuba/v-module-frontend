# Resend 邮件配置指南

## 🎯 为什么需要验证域名？

使用 `onboarding@resend.dev` 作为发件人时：
- **只能发送到注册 Resend 账号的邮箱**
- **邮件更容易被标记为垃圾邮件**（约 30% 的垃圾邮件率）

要发送到任意邮箱地址并提高送达率，必须验证你自己的域名。

## 📋 验证步骤

### 1. 在 Resend Dashboard 中添加域名

1. 登录 [Resend Dashboard](https://resend.com/domains)
2. 点击 **"Add Domain"** 或 **"Add"** 按钮
3. 输入你的域名（例如：`example.com` 或 `mail.example.com`）
4. 点击 **"Add"**

### 2. 配置 DNS 记录

Resend 会显示需要添加的 DNS 记录，通常包括：

#### SPF 记录（TXT 记录）
```
类型: TXT
名称: @ (或域名本身)
值: v=spf1 include:resend.com ~all
TTL: 3600 (或默认)
```

#### DKIM 记录（CNAME 记录）
通常需要添加 2 条 CNAME 记录，例如：
```
类型: CNAME
名称: resend._domainkey
值: resend._domainkey.resend.com
TTL: 3600
```

```
类型: CNAME
名称: resend._domainkey.yourdomain
值: (Resend 提供的特定值)
TTL: 3600
```

#### DMARC 记录（TXT 记录，可选但推荐）
```
类型: TXT
名称: _dmarc
值: v=DMARC1; p=none; rua=mailto:your-email@example.com
TTL: 3600
```

### 3. 在域名 DNS 提供商处添加记录

根据你的域名注册商/DNS 提供商：
- **Cloudflare**: DNS → Records → Add record
- **Namecheap**: Advanced DNS → Add New Record
- **GoDaddy**: DNS Management → Add Record
- **其他提供商**: 查找 DNS 设置或 DNS 管理页面

### 4. 等待 DNS 传播

- DNS 记录通常需要 **5 分钟到 48 小时** 才能生效
- 可以使用在线工具检查 DNS 记录是否正确：
  - [MXToolbox](https://mxtoolbox.com/)
  - [DNS Checker](https://dnschecker.org/)

### 5. 在 Resend 中验证域名

1. 返回 Resend Dashboard
2. 找到你添加的域名
3. 点击 **"Verify"** 或 **"Check DNS"** 按钮
4. Resend 会自动检查 DNS 记录是否正确配置
5. 如果所有记录都正确，域名状态会变为 **"Verified"**

### 6. 更新应用配置

域名验证完成后，更新 `.env` 文件：

```bash
# 使用你自己的域名邮箱作为发件人
RESEND_FROM=noreply@yourdomain.com
# 或者
RESEND_FROM=no-reply@yourdomain.com
```

### 7. 重启开发服务器

```bash
# 停止服务器（Ctrl+C）
# 重新启动
pnpm dev
```

## ✅ 验证域名后的优势

1. ✅ **可以发送到任意邮箱地址**（不再限制于注册邮箱）
2. ✅ **更高的邮件送达率**（约 95%，vs 70% 使用 onboarding@resend.dev）
3. ✅ **更少的垃圾邮件标记**（<5% vs ~30%）
4. ✅ **更专业的品牌形象**（使用自己的邮箱地址）
5. ✅ **更高的发送配额**（付费计划）

## 📧 邮件进入垃圾邮件文件夹？

如果邮件被标记为垃圾邮件：
1. 打开垃圾邮件文件夹找到邮件
2. 点击"这不是垃圾邮件"按钮
3. **验证域名**可以大幅降低垃圾邮件率（从 ~30% 降到 <5%）

## 🧪 测试

验证完成后，可以测试发送到任意邮箱：

1. 访问忘记密码页面
2. 输入任意邮箱地址（不限于注册邮箱）
3. 应该能够成功发送邮件

## 🔍 常见问题

### Q: 只有部署到生产环境后才能配置域名验证吗？

**A**: 不是的。域名验证可以在任何阶段进行：

- **开发环境**：如果你已经有域名，可以在开发阶段就验证域名，这样开发测试时邮件送达率也更好
- **生产环境**：部署后使用验证的域名，邮件送达率和专业度都更好

**如果你还没有域名**：
- 开发阶段：继续使用 `onboarding@resend.dev`，功能正常，只是邮件可能进入垃圾邮件文件夹
- 生产阶段：建议购买域名并验证，以获得更好的送达率和用户体验

### Q: 开发环境必须验证域名吗？

**A**: 不是必须的。开发环境可以：
- 使用 `onboarding@resend.dev`（功能正常，但可能进垃圾邮件）
- 使用注册邮箱进行测试（不会被标记为垃圾邮件）
- 如果已有域名，可以提前验证域名（推荐）

### Q: DNS 记录添加后仍然显示未验证？

**A**: 
- 等待更长时间（最长 48 小时）
- 检查 DNS 记录是否完全匹配（包括大小写、空格）
- 使用 DNS 查询工具确认记录已生效

### Q: 我没有自己的域名怎么办？

**A**: 
- **开发阶段**：可以使用 `onboarding@resend.dev`，功能正常
- **生产阶段**：建议购买域名（通常每年 $10-15，如 Namecheap、Cloudflare）
- 验证域名后可以显著提高送达率，避免垃圾邮件标记

### Q: 可以只验证子域名吗？

**A**: 可以。例如可以验证 `mail.example.com`，然后使用 `noreply@mail.example.com` 作为发件人。

### Q: 验证域名是免费的吗？

**A**: 
- 域名验证本身是免费的
- 但你需要有一个域名（通常需要购买，$10-15/年）
- Resend 免费计划每月有发送限制（通常是 100 封/天）

## 📊 开发 vs 生产环境建议

| 环境 | 发件人配置 | 送达率 | 建议 |
|------|-----------|--------|------|
| 开发环境（无域名） | `onboarding@resend.dev` | ~70% | ✅ 可以使用，功能正常 |
| 开发环境（有域名） | `noreply@yourdomain.com` | ~95% | ✅ 推荐，提前验证域名 |
| 生产环境 | `noreply@yourdomain.com` | ~95% | ✅ 必须，验证域名 |

**总结**：
- 开发环境不强制验证域名，但如果有域名，建议提前验证
- 生产环境强烈建议验证域名，以获得更好的用户体验和送达率

## 📚 相关资源

- [Resend 域名验证文档](https://resend.com/docs/dashboard/domains/introduction)
- [DNS 记录配置指南](https://resend.com/docs/dashboard/domains/dns-configuration)
- [Resend Dashboard](https://resend.com/domains)

