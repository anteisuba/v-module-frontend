# Database
# Format: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://postgres:password@localhost:5432/vtuber

# Session
# Generate a random string (at least 32 characters)
# Example: openssl rand -base64 32
SESSION_PASSWORD=replace-with-a-long-random-string-at-least-32-characters

# Email Service (Required for production)
# For development, these can be omitted (emails will be logged to console)
#
# 方式 1: Resend SDK (推荐，最简单)
# 注册账号: https://resend.com
# 获取 API Key: Dashboard -> API Keys -> Create API Key
RESEND_API_KEY=re_你的API_KEY
# 注意：使用 onboarding@resend.dev 只能发送到注册邮箱
# 要发送到任意邮箱，必须验证域名后使用：RESEND_FROM=noreply@yourdomain.com
RESEND_FROM=onboarding@resend.dev

# 方式 2: SMTP (备用方案)
# 如果设置了 RESEND_API_KEY，以下 SMTP 配置将被忽略
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM=noreply@yourdomain.com
#
# 详细配置指南请查看 document/SMTP_SETUP_GUIDE.md

# Application URL
# For production, set to your Vercel deployment URL
# Example: https://your-app.vercel.app
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Cloudflare R2 Storage (生产环境推荐)
# 免费额度：10GB 存储，1000万次读取/月
# 配置步骤：
# 1. 登录 Cloudflare Dashboard: https://dash.cloudflare.com
# 2. 进入 R2 -> Create bucket -> 输入 bucket 名称
# 3. 进入 R2 -> Manage R2 API Tokens -> Create API token
#    - 权限选择：Object Read & Write
#    - 资源选择：Specific bucket -> 选择你的 bucket
# 4. 复制 Account ID, Access Key ID, Secret Access Key
# 5. 配置 Public Access (可选)：
#    - 进入你的 bucket -> Settings -> Public Access
#    - 创建 Public URL，复制 URL
#
# 开发环境：如果未配置 R2，将使用本地文件系统 (public/uploads/)
# 生产环境：必须配置 R2，否则上传功能无法使用
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=your-bucket-name
# 可选：自定义公共域名（如果配置了 R2 自定义域名）
# R2_PUBLIC_URL=https://r2.yourdomain.com

